import React from 'react';
import PropTypes from 'prop-types';
import './style.scss'

index.propTypes = {
    
};

function index(props) {
    return (
        <div>
            
        </div>
    );
}

export default index;